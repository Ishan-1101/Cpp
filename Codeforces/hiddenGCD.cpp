#include <bits/stdc++.h>
using namespace std;

using vi  = vector<int>;
int main() {
   
    int ans = __gcd(18, 27); // hidden function, no need to code euclidean algo
    // cout<<ans;

    vi c;
    c.push_back(ans);
    cout<<c[0];

    return 0;
}